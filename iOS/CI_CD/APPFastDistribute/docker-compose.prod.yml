#服务器上的docker环境

services:
  iosappdelivery:
    image: iosappdelivery:latest
    ports:
      - "9000:8000"
    volumes:
      - ~/Downloads/uploads/debs_ipa:/data/debs_ipa
    environment:
      APP_ENV: prod
      SERVER_HOST: 192.168.81.25
      SERVER_PORT: 9000
      APP_NAME: 电e宝
      BUNDLE_ID: com.sgec.deb
      DEB_IPA_PATH: /data/debs_ipa